discord.py>=2.4
python-dotenv>=1.0
aiohttp>=3.9
# Only the SDK for the provider in use is needed; each is imported lazily.
# Grok goes over plain HTTP and needs neither.
google-genai>=1.0
anthropic>=0.116
# Event times are given in Beirut time; slim hosts lack the zone database.
tzdata
