# secrets
.env

# runtime state and logs (live on the host, not in git)
data/
logs/

# local env
.venv/
__pycache__/
.selfupdate-summary.md

# the owner's notes for Claude Code; never read by the self-update workflow
CLAUDE.local.md
